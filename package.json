{
  "name": "Wedding website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "tailwindcss -o ./tailwind.css --minify --watch",
    "build-css": "NODE_ENV=PRODUCTION tailwindcss -o ./tailwind.css --minify",
    "build": "bun run build-css && trunk build --release",
    "build-node": "npm run build-css && trunk build --release",
    "dev": "trunk serve --address=0.0.0.0 --watch ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brahms116/yew-demo.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.1.8"
  }
}
